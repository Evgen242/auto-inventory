name: Update README Stats

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  update-stats:
    runs-on: ubuntu-latest
    if: false  # Временно отключено, требует настройки API
    steps:
      - uses: actions/checkout@v4
      - name: Skip for now
        run: echo "Stats update disabled - requires API configuration"
